apiVersion: v2
name: nico-prereqs
description: >-
  Prerequisites for infra-controller/helm.
  Creates namespace, PostgreSQL, all required Secrets and ConfigMaps,
  bootstrap PKI, Vault PKI config, ESO resources, and ClusterIssuers.
type: application
version: 0.1.0
