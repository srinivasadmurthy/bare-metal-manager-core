# =============================================================================
# helm-prereqs/helmfile.yaml
# Full cluster operator + nico-prereqs orchestration.
#
# Usage (via setup.sh — do not call helmfile directly):
#   ./setup.sh
#
# Direct helmfile usage (individual releases):
#   helmfile sync -l name=cert-manager
#   helmfile sync -l name=vault
#   helmfile destroy
#
# Requires: helmfile, helm, kubectl, jq, ssh-keygen
#
# Required env:
#   REGISTRY_PULL_SECRET — registry pull secret / API key
#
# Hook/ordering notes:
#   - All multi-release sequencing is done in setup.sh (helmfile sync -l name=X
#     called in order), NOT via helmfile hooks or needs.
#   - helmfile v1.4 postSync hooks do NOT block dependent releases — avoid
#     using postSync hooks for cross-release sequencing.
#   - nico-prereqs release uses preSync/postSync hooks for local setup that
#     doesn't need cross-release ordering guarantees.
# =============================================================================

repositories:
  - name: jetstack
    url: https://charts.jetstack.io
  - name: hashicorp
    url: https://helm.releases.hashicorp.com
  - name: external-secrets
    url: https://charts.external-secrets.io
  - name: postgres-operator
    url: https://opensource.zalando.com/postgres-operator/charts/postgres-operator
  - name: metallb
    url: https://metallb.github.io/metallb
  - name: argo
    url: https://argoproj.github.io/argo-helm
  - name: nfd
    url: https://kubernetes-sigs.github.io/node-feature-discovery/charts

releases:
  # ---------------------------------------------------------------------------
  # MetalLB — LoadBalancer service provider (BGP or L2 mode).
  # Installed early (no TLS/PKI dependency). Must be up before NICo Core deploys
  # LoadBalancer services (nico-api, dhcp, dns, pxe, ssh-console-rs).
  #
  # Site-specific config (IPAddressPool, BGPPeer, BGPAdvertisement) is in
  # values/metallb-config.yaml and is applied by setup.sh after this installs.
  # ---------------------------------------------------------------------------
  - name: metallb
    namespace: metallb-system
    createNamespace: true
    chart: metallb/metallb
    version: "0.14.5"
    wait: true
    timeout: 600
    values:
      - operators/values/metallb.yaml

  # ---------------------------------------------------------------------------
  # Zalando postgres-operator — manages nico-pg-cluster HA Patroni cluster.
  # Installed before cert-manager (no TLS dependency). Must be up before
  # nico-prereqs so the postgresql.acid.zalan.do CRD exists when the chart
  # creates the postgresql resource.
  # ---------------------------------------------------------------------------
  - name: postgres-operator
    namespace: postgres
    createNamespace: true
    chart: postgres-operator/postgres-operator
    # NOTE: chart versions below 1.11.0 are no longer published in Zalando's chart
    # repository. The CRD API (acid.zalan.do/v1) is unchanged across this bump, and
    # nico-pg-cluster pins postgresql.version explicitly, so the database is unaffected.
    version: "1.11.0"
    wait: true
    timeout: 600
    values:
      - operators/values/postgres-operator.yaml

  # ---------------------------------------------------------------------------
  # cert-manager — certificate issuance and ClusterIssuers
  # No hooks: Prometheus CRDs and vault TLS bootstrap are done by setup.sh
  # phases 2b and 2c after this release installs.
  # ---------------------------------------------------------------------------
  - name: cert-manager
    namespace: cert-manager
    createNamespace: true
    chart: jetstack/cert-manager
    version: "v1.17.1"
    wait: true
    timeout: 600
    values:
      - operators/values/cert-manager.yaml

  # ---------------------------------------------------------------------------
  # HashiCorp Vault — HA Raft, 3 pods, TLS on 8200/8201
  # No hooks: vault TLS bootstrap (site-root CA + certs) is done by setup.sh
  # phase 2c before this release. IP SAN patch + init/unseal done by setup.sh
  # phases 3b and 4 after this release.
  # ---------------------------------------------------------------------------
  - name: vault
    namespace: vault
    createNamespace: true
    chart: hashicorp/vault
    version: "0.25.0"
    wait: true
    timeout: 600
    values:
      - operators/values/vault.yaml

  # ---------------------------------------------------------------------------
  # External Secrets Operator — syncs site-root CA → nico-roots
  # No hooks: installed by setup.sh phase 5 after vault is unsealed.
  # ---------------------------------------------------------------------------
  - name: external-secrets
    namespace: external-secrets
    createNamespace: true
    chart: external-secrets/external-secrets
    version: "0.14.3"
    wait: true
    timeout: 600
    values:
      - operators/values/external-secrets.yaml

  # ---------------------------------------------------------------------------
  # DPF prerequisites — Argo CD, Kamaji, maintenance-operator, NFD.
  # Installed by setup.sh phase 5b unless --skip-dpf (per-release
  # sync; nothing here installs on a normal run). Versions and values are
  # pinned from NVIDIA/doca-platform deploy/helmfiles/prereqs.yaml at the tag
  # matching NICO_DPF_VERSION (v26.4.0) — bump together.
  # All four install into dpf-operator-system, same as upstream.
  # NOTE: upstream pins cert-manager v1.19.3; we reuse our v1.17.1 release
  # above (compatible with DPF's Issuer/Certificate v1 usage — known skew).
  # ---------------------------------------------------------------------------
  - name: argo-cd
    namespace: dpf-operator-system
    createNamespace: true
    chart: argo/argo-cd
    version: "9.4.1"
    labels:
      group: dpf-prereqs
    wait: true
    timeout: 600
    values:
      - operators/values/argo-cd.yaml

  - name: kamaji
    namespace: dpf-operator-system
    createNamespace: true
    chart: oci://ghcr.io/nvidia/charts/kamaji
    version: "1.2.0"
    labels:
      group: dpf-prereqs
    wait: true
    timeout: 600
    values:
      - operators/values/kamaji.yaml
    hooks:
      # Upstream applies chart CRDs before sync (deploy/helmfiles/apply-crds.sh)
      # because helm only installs crds/ on first install, never on upgrade.
      - events: ["preSync"]
        command: "bash"
        args:
          - -c
          - |
            helm show crds oci://ghcr.io/nvidia/charts/kamaji --version 1.2.0 \
              | kubectl apply --server-side --field-manager=helmfile --force-conflicts -f - \
              || echo "WARNING: CRD apply for kamaji failed; continuing" >&2

  - name: maintenance-operator
    namespace: dpf-operator-system
    createNamespace: true
    chart: oci://ghcr.io/mellanox/maintenance-operator-chart
    version: "0.3.0"
    labels:
      group: dpf-prereqs
    wait: true
    timeout: 600
    values:
      - operators/values/maintenance-operator.yaml
    hooks:
      - events: ["preSync"]
        command: "bash"
        args:
          - -c
          - |
            helm show crds oci://ghcr.io/mellanox/maintenance-operator-chart --version 0.3.0 \
              | kubectl apply --server-side --field-manager=helmfile --force-conflicts -f - \
              || echo "WARNING: CRD apply for maintenance-operator failed; continuing" >&2

  - name: node-feature-discovery
    namespace: dpf-operator-system
    createNamespace: true
    chart: nfd/node-feature-discovery
    version: "0.18.3"
    labels:
      group: dpf-prereqs
    wait: true
    timeout: 600
    values:
      - operators/values/node-feature-discovery.yaml

  # ---------------------------------------------------------------------------
  # nico-prereqs — local Helm chart (this directory)
  # Installed by setup.sh phase 6 after vault + external-secrets are ready.
  #
  # values/nico-prereqs-dynamic.yaml.gotmpl: rendered at sync time — reads
  #   vault ClusterIP and root token live from the cluster via exec.
  #
  # nico-system namespace Helm ownership is set by unseal_vault.sh before this runs.
  # ssh-host-key is generated inline by Helm (_helpers.tpl genPrivateKey "ed25519").
  # postSync hook: labels nico-system for ESO selection, waits for nico-roots.
  # ---------------------------------------------------------------------------
  - name: nico-prereqs
    chart: "."
    namespace: nico-system
    createNamespace: true
    wait: true
    timeout: 600
    values:
      - values.yaml
      - values/nico-prereqs-dynamic.yaml.gotmpl
    hooks:
      - events: ["postSync"]
        command: "bash"
        args:
          - -c
          - |
            set -euo pipefail
            kubectl label namespace nico-system \
                nico.nvidia.com/managed=true --overwrite
            kubectl annotate externalsecret nico-roots-eso -n nico-system \
                force-sync="$(date +%s)" --overwrite 2>/dev/null || true
            echo "Waiting for nico-roots secret to be synced by ESO..."
            until kubectl get secret nico-roots -n nico-system &>/dev/null; do sleep 3; done
            echo "nico-roots ready"
