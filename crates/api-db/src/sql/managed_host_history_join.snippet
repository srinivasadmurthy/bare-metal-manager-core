LEFT JOIN LATERAL (
    SELECT json_agg(json_build_object('machine_id', mh.object_id, 'state', mh.state::TEXT, 'state_version', mh.state_version, 'timestamp', mh.timestamp)) AS json
    FROM machine_state_history mh
    WHERE mh.object_id = m2.id
) AS h ON true
