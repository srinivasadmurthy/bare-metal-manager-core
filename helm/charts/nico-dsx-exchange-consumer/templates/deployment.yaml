apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nico-dsx-exchange-consumer.name" . }}
  namespace: {{ include "nico-dsx-exchange-consumer.namespace" . }}
  labels:
    {{- include "nico-dsx-exchange-consumer.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "nico-dsx-exchange-consumer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nico-dsx-exchange-consumer.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "nico-dsx-exchange-consumer.name" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ include "nico-dsx-exchange-consumer.name" . }}
          image: "{{ include "nico-dsx-exchange-consumer.image" . }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          securityContext:
            capabilities:
              add:
                - SYS_PTRACE
          command:
            - /bin/sh
            - -c
            - |
              if [ -x /opt/nico/nico-dsx-exchange-consumer ]; then
                exec /opt/nico/nico-dsx-exchange-consumer
              else
                exec /opt/carbide/carbide-dsx-exchange-consumer
              fi
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          ports:
            - name: metrics
              containerPort: {{ .Values.service.port }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          resources: {}
          volumeMounts:
            - mountPath: "/var/run/secrets/spiffe.io"
              name: spiffe
              readOnly: true
      volumes:
        - name: spiffe
          secret:
            secretName: {{ include "nico-dsx-exchange-consumer.name" . }}-certificate
