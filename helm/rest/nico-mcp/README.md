# NICo MCP Helm Chart

By default, this chart deploys `nico-mcp` as a Kubernetes Deployment and
ClusterIP Service.

See the [NICo MCP Server operator guide](../../../docs/operations/nico-mcp.md)
for the supported topology, installation, values, configuration precedence,
and security requirements.
